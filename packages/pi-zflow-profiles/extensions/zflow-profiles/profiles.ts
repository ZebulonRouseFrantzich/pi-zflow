/**
 * profiles.ts — Profile loading, listing, and CRUD operations.
 *
 * **Phase 1 placeholder.**
 * The full profiles implementation will be part of Phase 2.
 *
 * TODO(phase-2): Implement profile loading and management.
 *   - Load profiles from `.pi/zflow-profiles.json` (project) and
 *     `~/.pi/agent/zflow-profiles.json` (user)
 *   - Merge with project-level profiles taking precedence
 *   - List available profiles
 *   - Set/get active profile
 *   - Cache active profile at `<user-state-dir>/active-profile.json`
 *   - Validate profile structure against ProfilesConfig type
 *
 * @module pi-zflow-profiles/profiles
 */

export {}
