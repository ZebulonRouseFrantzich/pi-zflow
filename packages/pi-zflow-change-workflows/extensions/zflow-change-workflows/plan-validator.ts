/**
 * plan-validator.ts — Plan validation before approval and implementation.
 *
 * Phase 7 validation is implemented in `orchestration.ts` via
 * `runPlanValidation()`, which validates canonical plan artifacts before
 * approval and lifecycle advancement. This legacy module is kept as a stable
 * extension-local import target for future extraction.
 *
 * @module pi-zflow-change-workflows/plan-validator
 */

export {}
