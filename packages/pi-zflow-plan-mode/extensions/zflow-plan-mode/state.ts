/**
 * state.ts — Plan-mode state tracking (active/inactive) and transition guards.
 *
 * **Phase 1 placeholder.**
 * The full state implementation will be part of Phase 2.
 *
 * TODO(phase-2): Implement plan mode state machine.
 *   - States: inactive → entering → active → exiting → inactive
 *   - Track which tools are restricted while active
 *   - Track entry/exit timestamps for diagnostics
 *   - Prevent nested activation
 *   - Emit zflow:planModeChanged events on state transitions
 *   - Integrate with bash-policy.ts for tool restriction enforcement
 *
 * @module pi-zflow-plan-mode/state
 */

export {}
